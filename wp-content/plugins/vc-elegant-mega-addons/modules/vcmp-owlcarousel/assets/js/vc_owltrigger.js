/*--------------------------------------------------
Owl Carousel
--------------------------------------------------*/
jQuery(document).ready(function($) {

      $(".owl-carousel").owlCarousel({
		
		items : 4,
		navigation : true, // Show next and prev buttons
		lazyLoad : true,
		loop:true,
		autoplay:true,
  
      });

});